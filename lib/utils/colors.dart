import 'package:flutter/material.dart';

// Main brand colors from logo
const primaryColor = Color(0xFF00AA3E);      // Green from R
const secondaryColor = Color(0xFF0000FF);    // Blue from Z
const tertiaryColor = Color(0xFFFF0000);     // Red from B

// Supporting colors
const indicatorColor = Color(0xFFE8F5E9);    // Light green for indicators
const ratingBarColor = Color(0xFF00AA3E);    // Using primary green for ratings

const wishListColor = Color(0xFFFF0000);     // Using logo red for wishlist

// Button colors
const territoryButtonColor = Color(0xFFE8F5E9);    // Light green background
const quaternaryButtonColor = Color(0xFFE3F2FD);   // Light blue background

// Scaffold and background colors
const scaffoldPrimaryLight = Color(0xFFF5F5F5);    // Light background
const scaffoldSecondaryDark = Color(0xFF1C1F26);   // Dark mode background
const appTextSecondaryColor = Color(0xFF6C757D);   // Text secondary color
const cardColor = Color(0xFFFFFFFF);               // White for cards
const borderColor = Color(0xFFE0E0E0);             // Border color
const appButtonColorDark = Color(0xFF282828);      // Dark mode button color
const lightPrimaryColor = Color(0xFF4CAF50);       // Lighter shade of primary green